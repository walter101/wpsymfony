{% extends 'basemanager.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <h1>Memorytypelist</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ memoryTypeList.id }}</td>
            </tr>
            <tr>
                <th>Memorytypeoption</th>
                <td>{{ memoryTypeList.memoryTypeOption }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('memorytypelist_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('memorytypelist_edit', { 'id': memoryTypeList.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{%  block menu %}
    {{  include('includes/menu.html.twig') }}
{%  endblock %}